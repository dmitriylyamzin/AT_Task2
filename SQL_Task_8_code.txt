SELECT * FROM 
    (SELECT EXTRACT(HOUR FROM TO_TIMESTAMP(QRY_QUEUE.C_IN_TIME, 'DD-MM-YYYY HH24:MI:SS' )) AS H, 
    COUNT(*) AS CNT FROM QRY_QUEUE 
    GROUP BY EXTRACT(HOUR FROM TO_TIMESTAMP(QRY_QUEUE.C_IN_TIME, 'DD-MM-YYYY HH24:MI:SS' ))
    ORDER BY COUNT(*) DESC)
    WHERE ROWNUM = 1;