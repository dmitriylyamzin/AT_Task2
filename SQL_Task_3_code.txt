SELECT QRY_TYPE.C_NAME AS QRYTYPE, ROUND ((1 - AVG(QRY_QUEUE.C_ST))*100, 2) AS PRC FROM QRY_QUEUE 
    JOIN QRY_TYPE ON QRY_QUEUE.C_QRY_TYPE = QRY_TYPE.ID
    GROUP BY QRY_TYPE.C_NAME
    HAVING (1 - AVG(QRY_QUEUE.C_ST))*100 > 5.0;