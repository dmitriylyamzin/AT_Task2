SELECT * FROM (SELECT ST_ABONENTS.C_NAME AS ABONENTNAME, COUNT(*) AS CNT FROM QRY_QUEUE 
    JOIN QRY_TYPE ON QRY_QUEUE.C_QRY_TYPE = QRY_TYPE.ID 
    JOIN ST_ABONENTS ON ST_ABONENTS.ID = QRY_TYPE.C_AB_REF
	GROUP BY ST_ABONENTS.C_NAME ORDER BY COUNT(*) DESC)
    WHERE ROWNUM <= 3;